# Program to count how many times an element appears in a tuple

numbers = (10, 20, 30, 20, 40, 20, 50)

element = 20
count = numbers.count(element)

print("Tuple:", numbers)
print(f"{element} appears {count} times.")
